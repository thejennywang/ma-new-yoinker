<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Yelperonni</title>
</head>
<body>
	<%= flash[:notice] %>
	<ul>
		<% if @restaurants.any? %>
			<% @restaurants.each do |restaurant| %>
				<li> <%= restaurant.name %> - <%= restaurant.category %> | <%= link_to 'Edit', edit_restaurant_path(restaurant) %> | <%=link_to 'Delete', restaurant_path(restaurant), method: :delete  %> </li>
			<% end %>
		<%else%>
			<p>No restaurants yet</p>
		<% end %>
		</ul>
	<%=link_to 'Add a restaurant', new_restaurant_path %>
</body>
</html>

<!-- data: {confirm: "Are you sure you want to delete this listing?"} -->