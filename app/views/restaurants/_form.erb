<%= form_for @restaurant do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name, placeholder: "Jenny's Cafe..." %>

		<%= f.label :category %>
		<%= f.text_field :category, placeholder: "Coffee, French..." %>

		<%= f.submit %>
	<% end %>