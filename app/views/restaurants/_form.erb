<%= form_for @restaurant do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name, placeholder: "Jenny's Cafe...", required: true %>

		<%= f.label :category %>
		<%= f.text_field :category, placeholder: "Coffee, French...", required: true %>

		<%= f.submit %>
	<% end %>