<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Review <%= @restaurant.name %></title>
</head>
<body>
	<h1><%= @restaurant.name %></h1>
	<%= form_for [@restaurant, @review] do |f| %>
	Your review for <%= @restaurant.name %>:
	<p><%= f.label :rating %>
	<%= f.select :rating,(1..5) %></p>
	<p><%= f.label :comment %> 
	<%= f.text_area :comment %></p>

	<%= f.submit 'Submit Review' %>
	<% end %>
</body>
</html>