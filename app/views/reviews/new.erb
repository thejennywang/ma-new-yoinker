<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Add a new review</title>
</head>
<body>
	<h1>Add a new review</h1>
	<%= form_for @review do |f| %>
		<%= f.label :rating %>
		<%= f.text_field :rating %>

		<%= f.label :comment %>
		<%= f.text_field :comment %>
		<%= f.hidden_field :restaurant_id, value: @review.restaurant_id %>

		<%= f.submit %>
	<% end %>
</body>
</html>